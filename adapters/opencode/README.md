# OpenCode adapter

Use reusable global or project agent definitions plus shared MCP configuration. The installed global `agentic-ecosystem` agent carries the policy header; it does not carry credentials or duplicate tool implementations.
