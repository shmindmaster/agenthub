# Factory Droid adapter

The canonical host id is `factory` (`Factory Droid`). Register shared MCP
servers through Factory's documented MCP configuration and use a Droid plugin
only when native command behavior is required. Keep capability-specific
plugins free of tokens.
