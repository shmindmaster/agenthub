# VS Code adapter

Validate the Claude-compatible plugin format before introducing an OpenPlugin duplicate. Use VS Code only as a host adapter: the capability contract remains in the registry and the MCP endpoint remains shared.

