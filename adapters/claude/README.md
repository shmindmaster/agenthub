# Claude Code adapter

Expose each capability through a capability-specific Claude plugin only when it has no current equivalent. Reuse the same MCP/API contract and environment variable names; never package credentials. Claude plugins may also be consumed by VS Code agent plugins.

