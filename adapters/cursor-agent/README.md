# Cursor Agent CLI adapter

- Launcher: `C:\Users\SaroshHussain\bin\cursor-agent.cmd`
- Runtime: `%LOCALAPPDATA%\cursor-agent\cursor-agent.cmd`
- Shared native home: `C:\Users\SaroshHussain\.cursor`
- Repository instructions: root `AGENTS.md`; Cursor-native agents and skills remain supported
- Autonomy mapping: launcher flags implement the configured repository-autonomous behavior
- Validation: `cursor-agent --version` and a non-mutating prompt only in a synthetic trusted workspace

Cursor IDE and Cursor Agent are separate retained surfaces sharing the same account/configuration ecosystem.
