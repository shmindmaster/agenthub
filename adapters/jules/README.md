# Google Jules adapter

- Native CLI: `%APPDATA%\npm\jules.cmd`
- Native state: `C:\Users\SaroshHussain\.jules`
- Classification: retained cloud-agent CLI
- Repository coordination: consume the repository `AGENTS.md`; no separate competing global policy
- Validation: npm package version plus a non-dispatching help command

Authentication and cloud task state remain unmanaged. Validation must never launch a billed or real task as a probe.
