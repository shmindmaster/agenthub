# Amp adapter

Consume MCP first. Build an Amp Bun/TypeScript plugin only for an Amp-only command, lifecycle hook, or interactive UI that MCP cannot express. Every such plugin must map to one registry capability.
