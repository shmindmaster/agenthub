# Local reports

Fleet inventory, drift, validation, and cleanup reports are machine-local runtime evidence and are ignored by Git. Generate them explicitly with `agentctl.ps1 <command> -WriteReport` when a durable local snapshot is needed.
