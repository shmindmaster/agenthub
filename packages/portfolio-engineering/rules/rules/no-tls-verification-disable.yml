id: no-tls-verification-disable
language: TypeScript
message: Process-wide TLS verification must not be disabled. Inspect provider-specific certificate requirements instead.
severity: error
rule:
  any:
    - pattern: process.env.NODE_TLS_REJECT_UNAUTHORIZED = '0'
    - pattern: process.env.NODE_TLS_REJECT_UNAUTHORIZED = "0"
