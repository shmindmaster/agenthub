# Demo production guide owner

The canonical guide is owned by the `product-demo-studio` capability. Invoke that skill only after
`prepare-product-for-demo` produces a validated final `DEMO-READY` handoff.
