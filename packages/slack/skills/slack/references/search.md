# Search

`slack_search` is the discovery tool. Pass a Slack search query, including
`in:<channel>` and `from:<@U...>`.

Results are canonical messages, not a prose digest. Follow a hit with
`slack_thread` when the work is "read every reply".
