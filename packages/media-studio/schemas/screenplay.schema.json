{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agenthub.local/schemas/media-studio/screenplay.json",
  "title": "Media studio screenplay",
  "type": "object",
  "additionalProperties": false,
  "required": ["title", "scenes"],
  "properties": {
    "title": { "type": "string", "minLength": 1 },
    "kind": { "type": "string" },
    "voice": { "type": "string" },
    "scenes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "narration"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "slug": { "type": "string" },
          "narration": { "type": "string", "minLength": 1 },
          "onScreen": { "type": "string" },
          "visualMode": {
            "type": "string",
            "enum": ["slide", "screen", "diagram", "talking-head", "motif", "animation", "narration-only"]
          },
          "action": { "type": "string" },
          "highlight": { "type": "string" },
          "durationSeconds": { "type": "number", "minimum": 0.5 },
          "truth": {
            "type": "string",
            "enum": ["FACT", "DRAMATIZED", "COMPOSITE"]
          }
        }
      }
    }
  }
}
