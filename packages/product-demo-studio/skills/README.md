Public video skills for this engine now live in `packages/media-studio`.

The former `product-demo*` skill trees are in `../pipeline/` so Sync-Capabilities does not deploy those names. Screencast scripts, agents, and schemas in this package remain the implementation media-studio invokes.
