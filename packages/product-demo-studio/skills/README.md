Public video skills for this engine now live in `packages/media-studio`.
Public slash command is media-studio `/video` only.

The former `product-demo*` skill trees and `/demo-*` command files are in `../pipeline/` so Sync-Capabilities and plugin slash-command discovery do not expose those names. Screencast scripts, agents, and schemas in this package remain the implementation media-studio invokes.
