Engine procedures for the screencast pipeline. Not slash commands.

Public video entry is media-studio `/video`. Load these files from
`media-studio` when a product-screencast job needs assessment, calibration,
or the full gated capture path.
