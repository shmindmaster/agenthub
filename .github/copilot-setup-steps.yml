name: Copilot setup
description: Configure the Copilot agent execution environment for this repository.
runs:
  using: composite
  steps:
    - name: Mark runner context
      run: |
        echo "repo: agenthub"
        echo "runner-host: $(hostname)"
        echo "runner-branch: ${GITHUB_REF_NAME:-unknown}"
